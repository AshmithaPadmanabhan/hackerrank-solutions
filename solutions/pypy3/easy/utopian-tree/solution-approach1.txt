# ──────────────────────────────────────────────────
# Link        https://www.hackerrank.com/challenges/utopian-tree/problem?isFullScreen=true
# Problem     Utopian Tree
# Difficulty  Easy
# Subdomain   Implementation
# Platform    HackerRank
# Language    pypy3
# Status      Accepted
# Submitted   2026-08-12, 11:58 a.m.
# Technique   iterative-cycle-simulation
# Time        O(n)
# Space       O(1)
# Insight     The tree height updates by doubling during odd-numbered cycles and incrementing by one during even-numbered cycles, starting from an initial height of one at cycle zero.
# Interview   Before: "I could use a mathematical formula to calculate the height based on parity." After: "I implemented an iterative simulation that runs in O(n) time, correctly handling the base case where n equals zero by returning the initial height of one."
# Pitfalls    (1) Misinterpreting the cycle parity by assuming spring is even instead of odd.  (2) Failing to handle the n equals zero case, which returns the initial height of one.  (3) Incorrectly initializing the loop range, leading to an extra or missing growth cycle.
# ──────────────────────────────────────────────────

#!/bin/python3

import math
import os
import random
import re
import sys

#
# Complete the 'utopianTree' function below.
#
# The function is expected to return an INTEGER.
# The function accepts INTEGER n as parameter.
#

def utopianTree(n):
    if n==0:
        return 1
    for i in range(n+1):
        if i==0:
            h=1
        elif (i%2)!=0:
            h=2*h
        else:
            h=h+1
    return h

if __name__ == '__main__':
    fptr = open(os.environ['OUTPUT_PATH'], 'w')

    t = int(input().strip())

    for t_itr in range(t):
        n = int(input().strip())

        result = utopianTree(n)

        fptr.write(str(result) + '\n')

    fptr.close()
